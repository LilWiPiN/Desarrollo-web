const toggleMenu = () => {
    const burgerMenu = document.querySelector(".menu-hamburger");
    const navigation = document.querySelector('.menu-navigation');

    navigation.classList.toggle('menu-navigation--mobile');
};
